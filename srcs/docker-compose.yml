networks:
  inception:
      driver: bridge

volumes:
  mariadb_data:
    driver: local

services:
  nginx:
    build: ./requirements/nginx
    container_name: nginx
    ports:
      - 443:443
    depends_on:
      mariadb:
        condition: service_healty
    env_file:
      - .env
    restart: always
    networks:
      - inception

  mariadb:
    buid: ./requirements/mariadb
    container_name: mariadb
    volumes:
      - mariadb_data:/var/lib/mysql
    env_file:
      - .env
    networks:
      - inception
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-uroot", "-p${SQL_ROOT_PASSWORD}"]
      interval: 10s
      retries: 5
      timeout: 5s
